---
date: "2022-01-10"
title: "Lodash 자주 사용하는 함수 정리"
tags: ["React", "TypeScript", "Lodash", "JavaScript"]
description: "자주 사용하는 lodash 함수 정리"
thumbnail: "https://github.com/coyo-hm/COYO-HM.github.io/assets/56423604/f68eeff0-6ee9-4ea9-9543-7046c91430b6"
published: true
---

<aside>
  📌 해당 포스트는 주기적으로 업데이트 됩니다.
</aside>

# Array 관련 함수

## `uniq`

> 배열 원소 중복 제거
>
> ```tsx
> uniq<T>(array: List<T> | null | undefined): T[];
> // arg: array
> // return: array
> ```

```tsx
const siteList = [0, 1, 1, 2];
const uniqSiteList = uniq(siteList); // [0, 1, 2]
```

# Collection 관련 함수

## `sortby`

> collection 값들을 원하는 필드를 기준으로 오름차순으로 정렬
>
> ```tsx
> sortBy(collection, [(iteratees = [_.identity])]);
>
> //collection: 정렬 대상
> //[iteratees=[_.identity]] 정렬 기준
>
> // => return 정렬된 새로운 array
> ```

- 여러 개의 필드값으로도 정렬 가능

```javascript
sortBy(정렬을 원하는 컬렉션, [정렬 기준 필드값 1, 정렬 기준 필드값 2]);
```

- 내림차 순으로 정렬하기 위해서는, `reverse()` 사용

```javascript
sortBy(정렬을 원하는 컬렉션, 정렬 기준 필드값).reverse();
```

```tsx
const users = [
  { user: "Fred", age: 48 },
  { user: "Barney", age: 36 },
  { user: "Fred", age: 40 },
  { user: "Barney", age: 34 },
];

const sortByFunc = sortBy(users, [
  function (o) {
    return o.user;
  },
]);
console.log(sortByFunc);
// => objects for [['Barney', 36], ['Barney', 34], ['Fred', 48], ['Fred', 40]]

const sortByTwoArg = sortBy(users, ["user", "age"]);
console.log(sortByTwoArg);
// => objects for [['Barney', 34], ['Barney', 36], ['Fred', 40], ['Fred', 48]]
```

# Lang 관련 함수

## `clonedeep`

> 객체 복사

# Object 관련 함수

## `get`

> 객체에서 원하는 값 찾기

```javascript
get(찾을 장소, 가져올 키, 기본 값);
```

# 참조

- [lodash 공식 문서](https://lodash.com/docs/4.17.15#compact)
