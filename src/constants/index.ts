const CONSTANTS = {
  DEFAULT_NUMBER_OF_RECENT_POST: 5,
  DEFAULT_NUMBER_OF_POST: {
    SERIES: 5,
    LIST: 6,
    CARD: 12,
  },
};

export const POST_TITLE_SELECTOR =
  ".markdown > h1, .markdown > h2, .markdown > h3, .markdown > h4, .markdown > h5, .markdown > h6";

export default CONSTANTS;
