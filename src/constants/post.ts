export const DEFAULT_NUMBER_OF_RECENT_POST = 5;
export const DEFAULT_NUMBER_OF_HOME_PROJECT = 3;
export const DEFAULT_NUMBER_OF_POST = {
  series: 5,
  list: 6,
  card: 12,
};

export const POST_TITLE_SELECTOR =
  ".markdown > h1, .markdown > h2, .markdown > h3, .markdown > h4, .markdown > h5, .markdown > h6";
